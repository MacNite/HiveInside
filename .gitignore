# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Build artifacts
*.uf2
*.hex
*.bin
*.elf
*/build
# nRF Connect build configurations, named after their build directories
# (docs/vscode-build.md). Directory-only patterns: these two names are generic
# enough that a file called `debug` should not be swept up with them.
*/debug/
*/lowpower/
*.log

# Editor
.vscode/
.idea/
*.swp
.DS_Store
*.code-workspace

# Claude Code delivery bundles
claude-bundles/
